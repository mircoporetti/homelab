---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: game-collector
  name: game-collector
  namespace: games-catalogue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: game-collector
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: game-collector
      annotations:
        prometheus.io/path: '/actuator/prometheus'
        prometheus.io/port: '8081'
        prometheus.io/scrape: 'true'
    spec:
      containers:
      - image: sp0re90/game-collector:latest
        name: game-collector
        imagePullPolicy: Always
        # readinessProbe:
        #   httpGet:
        #     scheme: HTTP
        #     path: /actuator/health
        #     port: 8080
        #   initialDelaySeconds: 240
        #   periodSeconds: 5
        #   timeoutSeconds: 5
        #   successThreshold: 1
        #   failureThreshold: 10
        # livenessProbe:
        #   httpGet:
        #     scheme: HTTP
        #     path: /actuator/health
        #     port: 8080
        #   initialDelaySeconds: 300
        #   periodSeconds: 60
        #   timeoutSeconds: 10
        #   successThreshold: 1
        #   failureThreshold: 3 
        securityContext:
          privileged: true
        ports:
        - containerPort: 8081
        #volumeMounts:
        #- name: config-volume
        #  mountPath: /app/config
        #volumes:
        #  - name: config-volume
        #    configMap:
        #      name: core-config-0.0.0-alpha