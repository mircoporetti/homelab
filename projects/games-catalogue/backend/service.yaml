---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: game-collector
  name: game-collector
  namespace: games-catalogue
spec:
  ports:
    - name: http
      port: 80
      targetPort: 8081
  selector:
    app: game-collector