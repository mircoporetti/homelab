---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: games-catalogue
  name: games-catalogue
  namespace: games-catalogue
spec:
  ports:
    - name: http
      port: 80
      targetPort: 3000
  selector:
    app: games-catalogue